class MusicModel {
  String? title, path, b_image, f_image;

  MusicModel({
    required this.title,
    required this.path,
    this.b_image,
    this.f_image,
  });
  factory MusicModel.fromJson(MusicModel musicModel) {
    return MusicModel(
      title: musicModel.title,
      path: musicModel.path,
      b_image: musicModel.b_image,
      f_image: musicModel.f_image,
    );
  }
}
